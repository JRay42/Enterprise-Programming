import { RepositoryApiList } from './repository-api-list';

describe('RepositoryApiList', () => {
  it('should create an instance', () => {
    expect(new RepositoryApiList()).toBeTruthy();
  });
});
