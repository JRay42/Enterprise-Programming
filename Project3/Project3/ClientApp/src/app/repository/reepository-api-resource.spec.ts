import { ReepositoryApiResource } from './reepository-api-resource';

describe('ReepositoryApiResource', () => {
  it('should create an instance', () => {
    expect(new ReepositoryApiResource()).toBeTruthy();
  });
});
