import { RepositoryIssueDetails } from './repository-issue-details';

describe('RepositoryIssueDetails', () => {
  it('should create an instance', () => {
    expect(new RepositoryIssueDetails()).toBeTruthy();
  });
});
