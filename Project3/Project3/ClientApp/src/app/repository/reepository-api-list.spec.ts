import { ReepositoryApiList } from './reepository-api-list';

describe('ReepositoryApiList', () => {
  it('should create an instance', () => {
    expect(new ReepositoryApiList()).toBeTruthy();
  });
});
