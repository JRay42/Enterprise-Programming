import { RepositoryCommitDetails } from './repository-commit-details';

describe('RepositoryCommitDetails', () => {
  it('should create an instance', () => {
    expect(new RepositoryCommitDetails()).toBeTruthy();
  });
});
