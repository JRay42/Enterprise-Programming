import { RepositoryApiResource } from './repository-api-resource';

describe('RepositoryApiResource', () => {
  it('should create an instance', () => {
    expect(new RepositoryApiResource()).toBeTruthy();
  });
});
